import {StyleSheet} from 'react-native';
import CColors from '../../color/CColors';
const styles = StyleSheet.create({
  textInputDiv: {
    flexDirection: 'row',

    justifyContent: 'center',
  },

  textInputContainerStyle: {width: '18%', marginRight: 10},
  textInputStyle: {textAlign: 'center', fontSize: 20},
  reSendCodeTextContainerStyle: {
    paddingVertical: 40,
  },
});
export default styles;
