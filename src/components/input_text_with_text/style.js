import {StyleSheet} from 'react-native';
import CColors from '../../color/CColors';
const styles = StyleSheet.create({
  container: {backgroundColor: CColors.white, paddingVertical: 15},
  textStyle: {fontSize: 18},
});
export default styles;
